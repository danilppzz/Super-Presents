package dev.danilppzz.superpresents.handler;

import org.bukkit.inventory.ItemStack;

public class ItemHandler {
    public static void createReward(ItemStack itemStack, String name) {
        // encode the item stack
        // insert reward into file with name: {name}
    }

    public static void deleteReward(String name) {
        // delete reward from file with name
    }

    public static void getReward(String name) {
        // get a reward from the file with name
    }
}
